//my task
import {city} from './scriptMod0.mjs'

// city = 'Kyiv'; // so mistake
let multi = (a, b) => a * b;

function goRight() {
  console.log(`We went to right of ${city} in the evening! `)
}
const name1 = 'John';
const name2 = 'Bill';

export default multi
export { name1, name2, goRight }
